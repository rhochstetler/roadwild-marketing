node_modules/

# GENERATED by scripts/sync-brand.mjs on every build, from the app's deployed
# CSS. Committing it would create a second copy of the brand in the repo, which
# is the exact thing the sync script exists to prevent.
site/assets/brand.css
